// Directory barrel — the RAW metadata IR layer consumed by decorators and seal.
export type { RawClassMeta, RawPropertyMeta, RuleDef, TransformDef, ExposeDef, TypeDef, MessageArgs } from './interfaces';
export { CollectionType } from './enums';
export { MetaStore, metaStore } from './meta-store';
