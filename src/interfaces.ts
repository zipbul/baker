// Re-export shim — interfaces moved to their owning domains (Phase C1). Importers repointed in C1b.
export type { SealOptions } from './seal/interfaces';
export type { RuntimeOptions } from './common/interfaces';
