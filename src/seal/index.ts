// Directory barrel — the compile stage's output, options, and entry point.
export type { SealedExecutors, SealOptions } from './interfaces';
export { sealRegistry } from './seal';
export { SEAL_OPTION_KEYS } from './constants';
