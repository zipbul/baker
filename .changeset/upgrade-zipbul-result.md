---
"@zipbul/baker": minor
---

Upgrade `@zipbul/result` from `^0.0.3` to `^0.1.4` and adopt `Result<T, E>` / `ResultAsync<T, E>` type aliases for cleaner type signatures. Fix seal placeholder to throw `SealError` instead of bare `Error`. Remove dead branch in deserialize input type guard.
